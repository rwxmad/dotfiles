# Ignored by install script and gnu stow

# git
\.git
\.gitignore
\.gitmodules
\.stow-local-ignore

# editor config
\.editorconfig
\.luarc\.json
stylua\.toml

# meta
^/README.*
^/LICENSE.*
pacman\.txt
build

# fonts (installed separately)
fonts

# install script itself
install

# handled separately by install
\.config
bin
